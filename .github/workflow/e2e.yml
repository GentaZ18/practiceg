name: Cypress tests for Brightline

on: [push]

jobs:
 cypress:
 runs-on: ubuntu-latest
 steps:
 - uses: actions/checkout@v2
 - uses: actions/setup-node@v1
  with:
   node-version: 12.x
 - name: install Dependencies
   run: npm install
 - name: Run Cypress tests
   run: npx cypress run



